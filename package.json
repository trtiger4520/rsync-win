{
  "name": "rsync-win-release",
  "version": "0.0.0-managed",
  "private": true,
  "description": "Release tooling for rsync-win. The .NET code is unaffected; this only exists so semantic-release can run in CI.",
  "devDependencies": {
    "@semantic-release/exec": "^6.0.3",
    "semantic-release": "^24.2.3"
  }
}
