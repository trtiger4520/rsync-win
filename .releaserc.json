{
  "branches": ["main"],
  "tagFormat": "v${version}",
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "dotnet publish src/RsyncWin.Cli -c Release -r win-x64 --self-contained -p:PublishSingleFile=true -p:Version=${nextRelease.version} -o publish/win-x64 && zip -j rsyncwin-v${nextRelease.version}-win-x64.zip publish/win-x64/rsyncwin.exe"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          {
            "path": "rsyncwin-v*-win-x64.zip",
            "label": "rsyncwin ${nextRelease.version} (win-x64 single-file)"
          }
        ]
      }
    ]
  ]
}
